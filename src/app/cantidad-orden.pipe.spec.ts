import { CantidadOrdenPipe } from './cantidad-orden.pipe';

describe('CantidadOrdenPipe', () => {
  it('create an instance', () => {
    const pipe = new CantidadOrdenPipe();
    expect(pipe).toBeTruthy();
  });
});
