import { TotalOrdenPipe } from './total-orden.pipe';

describe('TotalOrdenPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalOrdenPipe();
    expect(pipe).toBeTruthy();
  });
});
